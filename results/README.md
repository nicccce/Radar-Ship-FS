# Results Directory

本目录用于保存整理后的实验结果，真实结果文件默认不提交到 Git。

建议结构：

- `tables/`：主表、消融表、多随机种子统计；
- `figures/`：特征数、性能、稳定性和训练成本图；
- `models/`：必要的模型、特征掩码、缓存快照或最终 scorer 产物。

提交结果前请确认不包含私有数据、绝对路径、密钥或不可公开的中间样本。
