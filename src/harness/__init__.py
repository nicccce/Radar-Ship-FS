"""Harness package — the equal-footing frame both selection methods compose through.

Holds the common subset-production contract (COMPAT-001) that the classical baseline and the no-
trainer reinforced engine satisfy, and (in later tasks) the single-run orchestrator and run-artifact
emitter. The contract is consumed by TASK-202 (classical selector), TASK-203 (orchestrator), and
TASK-210 (engine).
"""
